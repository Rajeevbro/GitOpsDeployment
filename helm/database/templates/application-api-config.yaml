


{{- if .Values.environmentDetails}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.databaseName}}-config

type: Opaque
data:
  {{- range $key, $value := .Values.environmentDetails }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end}}
